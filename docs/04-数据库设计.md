# 梦见小程序 - 数据库设计

## 1. 数据库选型

| 数据库      | 用途       | 说明                             |
| ----------- | ---------- | -------------------------------- |
| MySQL 8.x   | 关系型数据 | 用户、订单、配置等结构化数据     |
| MongoDB 6.x | 文档数据   | 梦境内容、解析结果等非结构化数据 |
| Redis 7.x   | 缓存       | 会话、热点数据、限流计数         |

## 2. MySQL 表设计

### 2.1 用户表 (users)

```sql
CREATE TABLE `users` (
  `id` VARCHAR(32) NOT NULL COMMENT '用户ID',
  `open_id` VARCHAR(64) NOT NULL COMMENT '微信OpenID',
  `union_id` VARCHAR(64) DEFAULT NULL COMMENT '微信UnionID',
  `nickname` VARCHAR(64) DEFAULT '梦游者' COMMENT '昵称',
  `avatar` VARCHAR(512) DEFAULT NULL COMMENT '头像URL',
  `gender` TINYINT DEFAULT 0 COMMENT '性别 0未知 1男 2女',
  `phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号(加密)',
  `is_vip` TINYINT(1) DEFAULT 0 COMMENT '是否VIP',
  `vip_expire_at` DATETIME DEFAULT NULL COMMENT 'VIP过期时间',
  `lucky_points` INT DEFAULT 0 COMMENT '幸运值积分',
  `consecutive_days` INT DEFAULT 0 COMMENT '连续记梦天数',
  `last_dream_date` DATE DEFAULT NULL COMMENT '最后记梦日期',
  `total_dreams` INT DEFAULT 0 COMMENT '总梦境数',
  `total_tasks` INT DEFAULT 0 COMMENT '总任务完成数',
  `status` TINYINT DEFAULT 1 COMMENT '状态 1正常 0禁用',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_open_id` (`open_id`),
  KEY `idx_union_id` (`union_id`),
  KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

### 2.2 用户成就表 (user_achievements)

```sql
CREATE TABLE `user_achievements` (
  `id` BIGINT AUTO_INCREMENT COMMENT '自增ID',
  `user_id` VARCHAR(32) NOT NULL COMMENT '用户ID',
  `achievement_id` VARCHAR(32) NOT NULL COMMENT '成就ID',
  `unlocked_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '解锁时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_achievement` (`user_id`, `achievement_id`),
  KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户成就表';
```

### 2.3 积分记录表 (point_records)

```sql
CREATE TABLE `point_records` (
  `id` BIGINT AUTO_INCREMENT COMMENT '自增ID',
  `user_id` VARCHAR(32) NOT NULL COMMENT '用户ID',
  `type` ENUM('earn', 'consume') NOT NULL COMMENT '类型',
  `amount` INT NOT NULL COMMENT '积分数量',
  `balance` INT NOT NULL COMMENT '变动后余额',
  `source` VARCHAR(32) NOT NULL COMMENT '来源类型',
  `source_id` VARCHAR(64) DEFAULT NULL COMMENT '来源ID',
  `description` VARCHAR(128) DEFAULT NULL COMMENT '描述',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_created_at` (`created_at`),
  KEY `idx_source` (`source`, `source_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='积分记录表';
```

### 2.4 任务表 (tasks)

```sql
CREATE TABLE `tasks` (
  `id` VARCHAR(32) NOT NULL COMMENT '任务ID',
  `user_id` VARCHAR(32) NOT NULL COMMENT '用户ID',
  `dream_id` VARCHAR(32) NOT NULL COMMENT '关联梦境ID',
  `analysis_id` VARCHAR(32) NOT NULL COMMENT '关联解析ID',
  `type` VARCHAR(16) NOT NULL COMMENT '任务类型 wear/food/action',
  `content` VARCHAR(256) NOT NULL COMMENT '任务内容',
  `reward_points` INT DEFAULT 10 COMMENT '奖励积分',
  `status` ENUM('pending', 'completed', 'expired') DEFAULT 'pending' COMMENT '状态',
  `is_double_reward` TINYINT(1) DEFAULT 0 COMMENT '是否双倍奖励',
  `completed_at` DATETIME DEFAULT NULL COMMENT '完成时间',
  `expire_at` DATETIME NOT NULL COMMENT '过期时间',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_dream_id` (`dream_id`),
  KEY `idx_status_expire` (`status`, `expire_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='改运任务表';
```

### 2.5 权益兑换记录表 (orders)

> **说明**：此表已从付费订单改为积分兑换记录，适用于个人开发者（无企业资质）场景。

```sql
CREATE TABLE `orders` (
  `id` VARCHAR(32) NOT NULL COMMENT '兑换记录ID',
  `user_id` VARCHAR(32) NOT NULL COMMENT '用户ID',
  `order_no` VARCHAR(64) NOT NULL COMMENT '兑换单号',
  `type` ENUM('vip') NOT NULL COMMENT '兑换类型',
  `product_id` VARCHAR(32) NOT NULL COMMENT '权益ID',
  `product_name` VARCHAR(64) NOT NULL COMMENT '权益名称',
  `amount` DECIMAL(10, 2) NOT NULL COMMENT '消耗积分',
  `pay_amount` DECIMAL(10, 2) NOT NULL COMMENT '实际消耗积分（考虑折扣）',
  `status` ENUM('pending', 'paid', 'cancelled', 'refunded') DEFAULT 'pending' COMMENT '状态（paid=已兑换）',
  `pay_type` VARCHAR(16) DEFAULT NULL COMMENT '兑换方式（points/streak）',
  `transaction_id` VARCHAR(64) DEFAULT NULL COMMENT '保留字段',
  `paid_at` DATETIME DEFAULT NULL COMMENT '兑换时间',
  `expire_at` DATETIME NOT NULL COMMENT '权益过期时间',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_order_no` (`order_no`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_status` (`status`),
  KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='权益兑换记录表';
```

### 2.6 权益套餐表 (vip_plans)

> **说明**：此表已从付费套餐改为积分兑换项目，权益通过积分兑换或连续打卡获得，无需付费。

```sql
CREATE TABLE `vip_plans` (
  `id` VARCHAR(32) NOT NULL COMMENT '权益ID',
  `name` VARCHAR(32) NOT NULL COMMENT '权益名称',
  `duration_days` INT NOT NULL COMMENT '有效天数',
  `price` DECIMAL(10, 2) NOT NULL COMMENT '所需积分',
  `original_price` DECIMAL(10, 2) NOT NULL COMMENT '原价积分（用于展示折扣）',
  `sort_order` INT DEFAULT 0 COMMENT '排序',
  `is_active` TINYINT(1) DEFAULT 1 COMMENT '是否启用',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='权益套餐表';

-- 初始数据（积分兑换项目）
INSERT INTO `vip_plans` (`id`, `name`, `duration_days`, `price`, `original_price`, `sort_order`) VALUES
('ad_free_1day', '免广告特权', 1, 50, 50, 1),
('ad_free_7day', '免广告特权', 7, 280, 350, 2),
('ad_free_30day', '免广告特权', 30, 900, 1500, 3);
```

### 2.7 广告记录表 (ad_records)

```sql
CREATE TABLE `ad_records` (
  `id` BIGINT AUTO_INCREMENT COMMENT '自增ID',
  `user_id` VARCHAR(32) NOT NULL COMMENT '用户ID',
  `ad_type` VARCHAR(16) NOT NULL COMMENT '广告类型',
  `position` VARCHAR(32) NOT NULL COMMENT '广告位置',
  `event_type` VARCHAR(16) NOT NULL COMMENT '事件类型 show/complete/error',
  `duration` INT DEFAULT NULL COMMENT '观看时长(秒)',
  `error_msg` VARCHAR(256) DEFAULT NULL COMMENT '错误信息',
  `token` VARCHAR(64) DEFAULT NULL COMMENT '广告凭证',
  `token_used` TINYINT(1) DEFAULT 0 COMMENT '凭证是否已使用',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_token` (`token`),
  KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='广告记录表';
```

### 2.8 配置表 (configs)

```sql
CREATE TABLE `configs` (
  `id` INT AUTO_INCREMENT COMMENT '自增ID',
  `key` VARCHAR(64) NOT NULL COMMENT '配置键',
  `value` TEXT NOT NULL COMMENT '配置值(JSON)',
  `description` VARCHAR(256) DEFAULT NULL COMMENT '描述',
  `updated_at` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_key` (`key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='系统配置表';

-- 初始配置
INSERT INTO `configs` (`key`, `value`, `description`) VALUES
('dream_tags', '[{"id":"chase","name":"追逐","icon":"🏃"},{"id":"flying","name":"飞行","icon":"🦅"},{"id":"exam","name":"考试","icon":"📝"},{"id":"family","name":"亲人","icon":"👨‍👩‍👧"},{"id":"water","name":"水","icon":"💧"},{"id":"animal","name":"动物","icon":"🐕"},{"id":"lost","name":"迷路","icon":"🗺️"},{"id":"death","name":"死亡","icon":"💀"},{"id":"teeth","name":"牙齿","icon":"🦷"},{"id":"naked","name":"裸体","icon":"🙈"}]', '梦境标签配置'),
('emotions', '[{"id":"happy","name":"开心","icon":"😊"},{"id":"fear","name":"恐惧","icon":"😨"},{"id":"confused","name":"困惑","icon":"😕"},{"id":"sad","name":"悲伤","icon":"😢"}]', '情绪选项配置'),
('app_settings', '{"dreamMinLength":50,"dreamMaxLength":500,"reanalyzePointsCost":50}', '应用设置');
```

### 2.9 成就定义表 (achievements)

```sql
CREATE TABLE `achievements` (
  `id` VARCHAR(32) NOT NULL COMMENT '成就ID',
  `name` VARCHAR(64) NOT NULL COMMENT '成就名称',
  `description` VARCHAR(256) NOT NULL COMMENT '成就描述',
  `icon` VARCHAR(32) NOT NULL COMMENT '图标',
  `condition_type` VARCHAR(32) NOT NULL COMMENT '达成条件类型',
  `condition_value` INT NOT NULL COMMENT '达成条件值',
  `reward_points` INT DEFAULT 0 COMMENT '奖励积分',
  `sort_order` INT DEFAULT 0 COMMENT '排序',
  `is_active` TINYINT(1) DEFAULT 1 COMMENT '是否启用',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='成就定义表';

-- 初始数据
INSERT INTO `achievements` (`id`, `name`, `description`, `icon`, `condition_type`, `condition_value`, `reward_points`, `sort_order`) VALUES
('first_dream', '初次记梦', '记录第一个梦境', '🌙', 'total_dreams', 1, 10, 1),
('dreams_10', '入门梦想家', '累计记录10个梦境', '✨', 'total_dreams', 10, 20, 2),
('dreams_50', '资深梦想家', '累计记录50个梦境', '🌟', 'total_dreams', 50, 50, 3),
('dreams_100', '梦境大师', '累计记录100个梦境', '👑', 'total_dreams', 100, 100, 4),
('streak_3', '三日连续', '连续记梦3天', '🔥', 'consecutive_days', 3, 15, 5),
('streak_7', '周周有梦', '连续记梦7天', '🔥🔥', 'consecutive_days', 7, 30, 6),
('streak_30', '月度坚持', '连续记梦30天', '🔥🔥🔥', 'consecutive_days', 30, 100, 7),
('tasks_10', '任务新手', '完成10个改运任务', '🎯', 'total_tasks', 10, 20, 8),
('tasks_50', '任务达人', '完成50个改运任务', '🏆', 'total_tasks', 50, 50, 9);
```

## 3. MongoDB 集合设计

### 3.1 梦境集合 (dreams)

```javascript
// 集合: dreams
{
  _id: ObjectId("..."),                    // MongoDB ID
  dreamId: "dream_789",                    // 业务ID
  userId: "user_123456",                   // 用户ID
  content: "我梦见自己在一片草地上奔跑...",   // 梦境内容（当前版本）
  originalContent: "我梦见自己在一片草地上奔跑...", // 原始内容 (新增)
  currentVersionId: "ver_001",             // 当前版本ID (新增)
  tags: ["flying", "nature"],              // 标签
  emotion: "happy",                        // 情绪
  wordCount: 150,                          // 字数
  status: "analyzed",                      // 状态: pending/analyzed/deleted
  analysisId: "analysis_123",              // 关联解析ID

  // 隐私设置 (新增)
  isPublic: false,                         // 是否公开
  viewCount: 0,                            // 浏览次数
  likeCount: 0,                            // 点赞次数

  // 元数据
  meta: {
    platform: "mp-weixin",                 // 来源平台
    version: "1.0.0",                      // 客户端版本
    deviceInfo: {                          // 设备信息
      brand: "iPhone",
      model: "iPhone 14",
      system: "iOS 16.0"
    }
  },

  createdAt: ISODate("2026-01-21T08:00:00Z"),
  updatedAt: ISODate("2026-01-21T08:05:00Z"),
  deletedAt: null
}

// 索引
db.dreams.createIndex({ dreamId: 1 }, { unique: true })
db.dreams.createIndex({ userId: 1, createdAt: -1 })
db.dreams.createIndex({ userId: 1, status: 1 })
db.dreams.createIndex({ createdAt: -1 })
db.dreams.createIndex({ isPublic: 1, createdAt: -1 })  // 公开梦境查询 (新增)
db.dreams.createIndex({ isPublic: 1, tags: 1 })        // 标签筛选 (新增)
db.dreams.createIndex({ content: "text" })              // 全文索引
```

### 3.2 解析结果集合 (analyses)

```javascript
// 集合: analyses
{
  _id: ObjectId("..."),
  analysisId: "analysis_123",               // 业务ID
  dreamId: "dream_789",                     // 关联梦境ID
  userId: "user_123456",                    // 用户ID

  // 解析内容
  theme: "对自由的渴望与向往",                // 主题
  interpretation: "这个梦境反映了你内心对自由的渴望。草地象征着广阔的可能性...",

  // 运势
  fortune: {
    score: 85,                              // 运势评分
    tips: {
      career: "适合尝试新事物，把握机会主动出击",
      love: "保持开放心态，可能有意外惊喜",
      health: "精力充沛，多做户外运动"
    }
  },

  // AI相关
  ai: {
    model: "qwen-turbo",                    // 使用的模型
    promptVersion: "v1.2",                  // Prompt版本
    tokensUsed: 1500,                       // Token消耗
    latency: 3200,                          // 响应时间(ms)
    rawResponse: "...",                     // 原始响应(可选保留)
  },

  // 任务
  task: {
    taskId: "task_456",
    type: "action",
    content: "今天出门时深呼吸三次，感受自由的空气"
  },

  disclaimer: "本解析仅供娱乐参考，不构成任何专业建议",

  status: "completed",                      // processing/completed/failed
  createdAt: ISODate("2026-01-21T08:05:00Z"),
  updatedAt: ISODate("2026-01-21T08:05:00Z")
}

// 索引
db.analyses.createIndex({ analysisId: 1 }, { unique: true })
db.analyses.createIndex({ dreamId: 1 })
db.analyses.createIndex({ userId: 1, createdAt: -1 })
```

### 3.3 梦境版本集合 (dream_versions) (新增)

```javascript
// 集合: dream_versions
{
  _id: ObjectId("..."),
  versionId: "ver_001",                    // 版本ID
  dreamId: "dream_789",                    // 关联梦境ID
  userId: "user_123456",                   // 用户ID

  type: "original",                        // 版本类型: original/polished
  content: "我梦见自己在一片草地上奔跑...",  // 版本内容
  wordCount: 150,                          // 字数

  // 润色相关（仅polished类型有）
  polishedFrom: null,                      // 基于哪个版本润色
  polishPrompt: null,                      // 使用的Prompt
  aiModel: null,                           // 使用的AI模型
  tokensUsed: null,                        // Token消耗

  // 版本信息
  versionNumber: 1,                        // 版本号
  isCurrent: false,                        // 是否为当前版本

  createdAt: ISODate("2026-01-21T08:00:00Z"),
  updatedAt: ISODate("2026-01-21T08:00:00Z")
}

// 索引
db.dream_versions.createIndex({ versionId: 1 }, { unique: true })
db.dream_versions.createIndex({ dreamId: 1, createdAt: -1 })
db.dream_versions.createIndex({ userId: 1, type: 1 })
```

### 3.4 公开梦境浏览记录集合 (explore_views) (新增)

```javascript
// 集合: explore_views
{
  _id: ObjectId("..."),
  viewerId: "user_123456",                 // 浏览者ID
  dreamId: "dream_456",                    // 梦境ID
  authorId: "user_xxx",                    // 作者ID

  // 浏览信息
  viewDuration: 30,                        // 浏览时长（秒）
  isLiked: false,                          // 是否点赞

  // 来源
  source: "random",                        // 来源：random/filter/search

  createdAt: ISODate("2026-01-21T10:00:00Z")
}

// 索引
db.explore_views.createIndex({ viewerId: 1, createdAt: -1 })
db.explore_views.createIndex({ dreamId: 1, createdAt: -1 })
db.explore_views.createIndex({ createdAt: 1 }, { expireAfterSeconds: 15552000 })  // 180天自动删除
```

### 3.5 操作日志集合 (operation_logs)

```javascript
// 集合: operation_logs
{
  _id: ObjectId("..."),
  userId: "user_123456",
  action: "dream_submit",                   // 操作类型
  target: {
    type: "dream",
    id: "dream_789"
  },
  params: {                                 // 请求参数(脱敏)
    wordCount: 150,
    tagsCount: 2
  },
  result: {
    success: true,
    code: 0
  },
  meta: {
    ip: "xxx.xxx.xxx.xxx",
    userAgent: "...",
    platform: "mp-weixin",
    requestId: "req_xxx"
  },
  createdAt: ISODate("2026-01-21T08:00:00Z")
}

// 索引
db.operation_logs.createIndex({ userId: 1, createdAt: -1 })
db.operation_logs.createIndex({ action: 1, createdAt: -1 })
db.operation_logs.createIndex({ createdAt: 1 }, { expireAfterSeconds: 7776000 })  // 90天自动删除
```

## 4. Redis 数据结构

### 4.1 Key 命名规范

```
{业务域}:{数据类型}:{标识符}:{子项}

例如:
user:info:user_123456           # 用户信息
dream:list:user_123456          # 梦境列表
rate:dream_submit:user_123456   # 梦境提交限流
session:token_xxx               # 会话信息
```

### 4.2 常用数据结构

```redis
# 1. 用户信息缓存 (String + JSON)
SET user:info:user_123456 '{"id":"user_123456","nickname":"...","isVip":false,...}'
EXPIRE user:info:user_123456 86400  # 24小时

# 2. 用户Token (String)
SET session:token_xxx "user_123456"
EXPIRE session:token_xxx 7200  # 2小时

# 3. 今日梦境标记 (String)
SET dream:today:user_123456:2026-01-21 "dream_789"
EXPIREAT dream:today:user_123456:2026-01-21 <当日24点时间戳>

# 4. 连续记梦计数 (Hash)
HSET user:streak:user_123456 consecutive_days 7
HSET user:streak:user_123456 last_date "2026-01-20"

# 5. 用户梦境日历 (BitMap)
SETBIT dream:calendar:user_123456:2026-01 20 1   # 1月20日有记梦
SETBIT dream:calendar:user_123456:2026-01 21 1   # 1月21日有记梦

# 6. 限流计数 (String + INCR)
SET rate:dream_submit:user_123456 1
EXPIRE rate:dream_submit:user_123456 86400  # 每日重置

# 7. 排行榜 (Sorted Set)
ZADD leaderboard:streak 7 user_123456
ZADD leaderboard:streak 14 user_234567

# 8. 配置缓存 (Hash)
HSET config:app dream_tags '[...]'
HSET config:app emotions '[...]'

# 9. 广告Token (String)
SET ad:token:ad_token_xxx '{"userId":"user_123456","position":"before_analysis"}'
EXPIRE ad:token:ad_token_xxx 300  # 5分钟有效
```

## 5. 数据迁移与备份

### 5.1 备份策略

| 数据库  | 备份方式   | 频率     | 保留时间 |
| ------- | ---------- | -------- | -------- |
| MySQL   | 全量备份   | 每日凌晨 | 30天     |
| MySQL   | binlog同步 | 实时     | 7天      |
| MongoDB | 副本集     | 实时     | -        |
| MongoDB | 快照备份   | 每日     | 7天      |
| Redis   | RDB        | 每小时   | 24小时   |
| Redis   | AOF        | 实时     | 24小时   |

### 5.2 数据归档

```sql
-- 历史数据归档（超过1年的数据）
-- 每月执行一次，将旧数据迁移到归档表

-- 梦境数据归档
INSERT INTO dreams_archive SELECT * FROM dreams
WHERE created_at < DATE_SUB(NOW(), INTERVAL 1 YEAR);

DELETE FROM dreams
WHERE created_at < DATE_SUB(NOW(), INTERVAL 1 YEAR);
```

## 6. ER 图

```
┌──────────────┐       ┌──────────────┐       ┌──────────────┐
│    users     │       │    dreams    │       │  analyses    │
├──────────────┤       ├──────────────┤       ├──────────────┤
│ id (PK)      │──────<│ user_id (FK) │       │ analysis_id  │
│ open_id      │       │ dream_id     │──────<│ dream_id (FK)│
│ nickname     │       │ content      │       │ user_id (FK) │
│ is_vip       │       │ tags         │       │ theme        │
│ lucky_points │       │ emotion      │       │ interpretation│
└──────────────┘       │ status       │       │ fortune      │
        │              └──────────────┘       └──────────────┘
        │                     │                      │
        │                     │                      │
        ▼                     ▼                      ▼
┌──────────────┐       ┌──────────────┐       ┌──────────────┐
│point_records │       │    tasks     │       │  ad_records  │
├──────────────┤       ├──────────────┤       ├──────────────┤
│ user_id (FK) │       │ user_id (FK) │       │ user_id (FK) │
│ type         │       │ dream_id (FK)│       │ ad_type      │
│ amount       │       │ content      │       │ event_type   │
│ source       │       │ status       │       │ token        │
└──────────────┘       └──────────────┘       └──────────────┘
        │
        │
        ▼
┌──────────────┐       ┌──────────────┐       ┌──────────────┐
│   orders     │       │  vip_plans   │       │   configs    │
├──────────────┤       ├──────────────┤       ├──────────────┤
│ user_id (FK) │       │ id (PK)      │       │ key          │
│ product_id   │──────>│ name         │       │ value        │
│ amount       │       │ price        │       │ description  │
│ status       │       │ duration_days│       └──────────────┘
└──────────────┘       └──────────────┘
```

## 7. Prisma Schema

```prisma
// prisma/schema.prisma

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id              String    @id @default(cuid())
  openId          String    @unique @map("open_id")
  unionId         String?   @map("union_id")
  nickname        String    @default("梦游者")
  avatar          String?
  gender          Int       @default(0)
  phone           String?
  // 特权状态：通过连续打卡/积分兑换获得，非付费
  isVip           Boolean   @default(false) @map("is_vip")
  vipExpireAt     DateTime? @map("vip_expire_at") // 特权过期时间
  luckyPoints     Int       @default(0) @map("lucky_points") // 幸运积分，用于兑换权益
  consecutiveDays Int       @default(0) @map("consecutive_days")
  lastDreamDate   DateTime? @map("last_dream_date") @db.Date
  totalDreams     Int       @default(0) @map("total_dreams")
  totalTasks      Int       @default(0) @map("total_tasks")
  status          Int       @default(1)
  createdAt       DateTime  @default(now()) @map("created_at")
  updatedAt       DateTime  @updatedAt @map("updated_at")

  tasks         Task[]
  orders        Order[]
  pointRecords  PointRecord[]
  adRecords     AdRecord[]
  achievements  UserAchievement[]

  @@map("users")
}

model Task {
  id             String    @id @default(cuid())
  userId         String    @map("user_id")
  dreamId        String    @map("dream_id")
  analysisId     String    @map("analysis_id")
  type           String
  content        String
  rewardPoints   Int       @default(10) @map("reward_points")
  status         TaskStatus @default(pending)
  isDoubleReward Boolean   @default(false) @map("is_double_reward")
  completedAt    DateTime? @map("completed_at")
  expireAt       DateTime  @map("expire_at")
  createdAt      DateTime  @default(now()) @map("created_at")

  user User @relation(fields: [userId], references: [id])

  @@index([userId])
  @@index([dreamId])
  @@index([status, expireAt])
  @@map("tasks")
}

model PointRecord {
  id          BigInt   @id @default(autoincrement())
  userId      String   @map("user_id")
  type        PointType
  amount      Int
  balance     Int
  source      String
  sourceId    String?  @map("source_id")
  description String?
  createdAt   DateTime @default(now()) @map("created_at")

  user User @relation(fields: [userId], references: [id])

  @@index([userId])
  @@index([createdAt])
  @@map("point_records")
}

model Order {
  id            String      @id @default(cuid())
  userId        String      @map("user_id")
  orderNo       String      @unique @map("order_no")
  type          OrderType
  productId     String      @map("product_id")
  productName   String      @map("product_name")
  amount        Decimal     @db.Decimal(10, 2)
  payAmount     Decimal     @map("pay_amount") @db.Decimal(10, 2)
  status        OrderStatus @default(pending)
  payType       String?     @map("pay_type")
  transactionId String?     @map("transaction_id")
  paidAt        DateTime?   @map("paid_at")
  expireAt      DateTime    @map("expire_at")
  createdAt     DateTime    @default(now()) @map("created_at")
  updatedAt     DateTime    @updatedAt @map("updated_at")

  user User @relation(fields: [userId], references: [id])

  @@index([userId])
  @@index([status])
  @@map("orders")
}

// 权益套餐表（原VIP套餐表，现用于定义可兑换的权益）
// 权益通过积分兑换或连续打卡获得，不再需要付费
model VipPlan {
  id           String   @id // 权益ID: ad_free_1day, ad_free_7day 等
  name         String   // 权益名称
  durationDays Int      @map("duration_days") // 有效天数
  price        Decimal  @db.Decimal(10, 2) // 所需积分
  originalPrice Decimal @map("original_price") @db.Decimal(10, 2) // 原价积分（用于展示折扣）
  sortOrder    Int      @default(0) @map("sort_order")
  isActive     Boolean  @default(true) @map("is_active")
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")

  @@map("vip_plans")
}

model AdRecord {
  id        BigInt   @id @default(autoincrement())
  userId    String   @map("user_id")
  adType    String   @map("ad_type")
  position  String
  eventType String   @map("event_type")
  duration  Int?
  errorMsg  String?  @map("error_msg")
  token     String?
  tokenUsed Boolean  @default(false) @map("token_used")
  createdAt DateTime @default(now()) @map("created_at")

  user User @relation(fields: [userId], references: [id])

  @@index([userId])
  @@index([token])
  @@map("ad_records")
}

model Achievement {
  id             String  @id
  name           String
  description    String
  icon           String
  conditionType  String  @map("condition_type")
  conditionValue Int     @map("condition_value")
  rewardPoints   Int     @default(0) @map("reward_points")
  sortOrder      Int     @default(0) @map("sort_order")
  isActive       Boolean @default(true) @map("is_active")

  users UserAchievement[]

  @@map("achievements")
}

model UserAchievement {
  id            BigInt   @id @default(autoincrement())
  userId        String   @map("user_id")
  achievementId String   @map("achievement_id")
  unlockedAt    DateTime @default(now()) @map("unlocked_at")

  user        User        @relation(fields: [userId], references: [id])
  achievement Achievement @relation(fields: [achievementId], references: [id])

  @@unique([userId, achievementId])
  @@index([userId])
  @@map("user_achievements")
}

model Config {
  id          Int      @id @default(autoincrement())
  key         String   @unique
  value       String   @db.Text
  description String?
  updatedAt   DateTime @updatedAt @map("updated_at")

  @@map("configs")
}

enum TaskStatus {
  pending
  completed
  expired
}

enum PointType {
  earn
  consume
}

enum OrderType {
  vip
}

enum OrderStatus {
  pending
  paid
  cancelled
  refunded
}
```
